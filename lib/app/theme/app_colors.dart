import 'package:flutter/material.dart';

abstract final class AppColors {
  // ─────────────────────────────────────────────
  // Background
  // ─────────────────────────────────────────────

  static const background = Color(0xFF0B0D10);

  static const surface = Color(0xFF111419);

  static const surfaceElevated = Color(0xFF171A20);

  static const surfaceHighlight = Color(0xFF1C2027);

  // ─────────────────────────────────────────────
  // Borders
  // ─────────────────────────────────────────────

  static const border = Color(0xFF242832);

  static const borderSubtle = Color(0xFF1D2128);

  // ─────────────────────────────────────────────
  // Primary
  // ─────────────────────────────────────────────
  static const primary = Color(0xFFC4B395);

  static const primarySoft = Color(0xFFD6C9B2);

  static const primaryDark = Color(0xFFA99572);
  static const primaryContainer = Color(0xFF211B3A);

  // ─────────────────────────────────────────────
  // Text
  // ─────────────────────────────────────────────

  static const textPrimary = Color(0xFFF5F7FA);

  static const textSecondary = Color(0xFF9CA3AF);

  static const textMuted = Color(0xFF69707D);

  static const textDisabled = Color(0xFF454B55);

  // ─────────────────────────────────────────────
  // Semantic
  // ─────────────────────────────────────────────

  static const success = Color(0xFF35C98A);

  static const successContainer = Color(0xFF123528);

  static const warning = Color(0xFFF4B740);

  static const warningContainer = Color(0xFF382E17);

  static const error = Color(0xFFFF5C6C);

  static const errorContainer = Color(0xFF3A191F);

  static const info = Color(0xFF4DA3FF);

  static const infoContainer = Color(0xFF162C43);

  // ─────────────────────────────────────────────
  // Utility
  // ─────────────────────────────────────────────

  static const transparent = Colors.transparent;

  static const white = Colors.white;

  static const black = Colors.black;



  static const Color timetable = Color(0xFF00B8A9);
  static const Color marks = Color(0xFFFF9F43);
  static const Color attendance = Color(0xFFEE5A6F);
  static const Color more = Color(0xFF7367F0);
}
