name: Build & Release APK

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Check Flutter version
        run: flutter --version && dart --version

      - name: Get dependencies
        run: flutter pub get

      # Build Universal APK
      - name: Build Universal APK
        run: flutter build apk --release

      # Build Split APKs
      - name: Build APKs per ABI
        run: flutter build apk --release --split-per-abi

      # Rename APKs
      - name: Rename APKs
        run: |
          mv build/app/outputs/flutter-apk/app-release.apk alfred.apk
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk alfred-arm64.apk
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk alfred-armv7.apk
          mv build/app/outputs/flutter-apk/app-x86_64-release.apk alfred-x86_64.apk

      # Create GitHub Release
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            alfred.apk
            alfred-arm64.apk
            alfred-armv7.apk
            alfred-x86_64.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}